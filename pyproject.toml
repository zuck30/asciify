[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "asciify"
version = "0.1.0"
description = "A simple CLI tool to generate ASCII art."
dependencies = [
    "pyfiglet",
]

[project.scripts]
asciify = "asciify.cli:main"